# Trousers

## Current implementation (pending verification)

In `layout.ts` and `index.html`:

```
legFW = max(hip/4 + 0.75,  thigh × 0.42 + 0.75)   // front leg width
legBW = max(hip/4 + 2.5,   thigh × 0.55 + 0.75)   // back leg width
trsH  = outseam + cuffDepth × 2                     // piece height
```

### Open questions
- Are the front/back leg width formulas correct?
- How should rise affect the piece dimensions?

---

<!-- Add formulas here as references are confirmed -->
